# Optional. When omitted, the app runs in a fully functional demo mode.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<database>

# Optional. When omitted, the server uses a deterministic portfolio-demo assistant.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

PORT=3001
